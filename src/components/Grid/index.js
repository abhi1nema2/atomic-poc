import React from 'react';
import './styles.scss';

function Grid(props) {
    const { Heading } = props;

    return (
        <div className="grid">
            Heelo
        </div>
    )
}

export default Grid;
